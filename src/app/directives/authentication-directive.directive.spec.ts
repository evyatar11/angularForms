import { AuthenticationDirectiveDirective } from './authentication-directive.directive';

describe('AuthenticationDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new AuthenticationDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
