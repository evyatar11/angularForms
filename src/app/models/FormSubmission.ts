import {SubmittedFormRawData} from './SubmittedFormRawData';

export class FormSubmission{
  accountOfficeName: string;
  customerName: string;
  borrowerRating: string;
  pdScore: string;
  submittedFormRawData: SubmittedFormRawData;
}
